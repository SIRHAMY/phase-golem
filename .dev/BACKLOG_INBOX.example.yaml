# BACKLOG_INBOX.yaml — Drop items here to be ingested on next phase-golem run.
# The file is deleted after ingestion. Only `title` is required.
#
# Copy this file to BACKLOG_INBOX.yaml and edit.

# Minimal item (just a title):
- title: "Fix the flaky timeout in shutdown test"

# Full item with all optional fields:
- title: "Add CSV export to reports"
  description: "Users want to export pipeline run history as CSV for external dashboards"
  size: small          # small | medium | large
  risk: low            # low | medium | high
  impact: high         # low | medium | high
  pipeline_type: feature
  dependencies:
    - WRK-042
